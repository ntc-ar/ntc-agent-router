interface:
  display_name: "NTC Agent Router"
  short_description: "Choose agents, models and effort for each task"
  default_prompt: "Use $ntc-agent-router to complete this task with delegation and effort matched to its difficulty."
policy:
  allow_implicit_invocation: true
